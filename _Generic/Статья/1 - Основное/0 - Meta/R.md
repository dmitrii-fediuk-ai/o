#
`A⁂` ≔ ⠿~ ⟨ Мой proposal `ꆜ` для `P⁎` ⟩

# `Aᨀ`
`Aᨀ` ≔ ⟨ Текущая редакция `A⁂` ⟩

# `ⵣ⠿`
`ⵣ⠿` ≔ ⠿~ ⟨ Прошлые (устаревшие) редакции `A⁂` ⟩

##
`ⵣᵢ` : `ⵣ⠿`

# Aᨀ⧙…⧘
## 
`Aᨀ⧙P₁, P₂, …, Pₙ⧘` ≔ ⠿ ⟨ Пункты `Aᨀ` с номерами `P₁, P₂, …, Pₙ`  ⟩

## 
`Aᨀ⧙P₁-Pₙ⧘` ≔ ⠿ ⟨ Пункты `Aᨀ` с номерами от `P₁` до `Pₙ` включительно ⟩
```
Если в качестве `P₁` указан `0` — это означает «начало `Aᨀ`»
```

## 
`Aᨀ⧙D⧘` ≔ ⟨ Фрагмент `Aᨀ`⟩
```
`D` либо указывает конкретный пункт `Aᨀ`,  либо словесно описывает границы фрагмента. 
```

# `𐒌ᨀ⠿`
## 
`𐒌ᨀ⠿` ≔ ⠿~ ⟨ Замечания к `Aᨀ` ⟩ 

##
`𐒌ᨀᵢ` : `𐒌ᨀ⠿`

###
ꘖ `𐒌ᨀᵢ` ∋ ⟨ `Points` ⟩ ⟪ Перечень пунктов `Aᨀ`, к которым относится `𐒌ᨀᵢ` ⟫

###
ꘖ `𐒌ᨀᵢ` ∋ ⟨ `Citations` ⟩ 
```
Конкретные цитаты из `Aᨀ`, к которым относится `𐒌ᨀᵢ`.
Эти цитаты должны быть законченными предложениями. 
```

###
ꘖ `𐒌ᨀᵢ` ∋ ⟨ `Essence` ⟩ ⟪ Твой текст `𐒌ᨀᵢ` ⟫

###
ꘖ `𐒌ᨀᵢ` ∋ ⟨ `Confidence` ⟩ 
```
Твоя степень уверенности в истинноcти твоего `𐒌ᨀᵢ` по шкале от 0 до 100:
- 0 означает, что `𐒌ᨀᵢ` безосновательно (ошибочно).
- 100 означает, что ты полностью уверен в правоте своего `𐒌ᨀᵢ`.
```

###
ꘖ `𐒌ᨀᵢ` ∋ ⟨ `Ontology.NewRules` ⟩ 
```
#
Новые знания (сформулированные в виде правил), которые надо добавить к `᛭E` (помимо правок `Aᨀ`), чтобы впредь не повторять `𐒌ᨀᵢ`.
```

###
ꘖ `𐒌ᨀᵢ` ∋ ⟨ `Ontology.Corrections` ⟩ 
```
#
Правки, которые надо внести в текущие знания `᛭E` (помимо `𐒌ᨀᵢ「Ontology.NewRules」` и правок `Aᨀ`), чтобы впредь не повторять `𐒌ᨀᵢ`.
```

#
Не пиши в `𐒌ᨀᵢ「Ontology.NewRules」` и `𐒌ᨀᵢ「Ontology.Corrections」` правки к `Aᨀ`.

#
Не используй в `𐒌ᨀᵢ「Ontology.NewRules」` и `𐒌ᨀᵢ「Ontology.Corrections」` сокращения из `Aᨀ` (в частности, из секции «Key definitions» `Aᨀ` и из других мест, где они вводятся в `Aᨀ`).
Сокращения из `Aᨀ` предназначены для `ꆜ`.
Правила `᛭E` должны быть понятны читателю без контекста `Aᨀ`.
Поэтому вводи в `᛭E` свои обозначения, используя синтаксис `᛭L`.
Используй для обозначий формат `Eⵂ<номер>`, где `<номер>` — целочисленный порядковый номер обозначения со скозной нумерацией внутри `᛭E`.

#
`𐒌ᨀᵢ「Ontology.NewRules」` ⋃ `𐒌ᨀᵢ「Ontology.Corrections」` не может быть пустым множеством: иначе`᛭E` не получит новых знаний для избежания повторений `𐒌ᨀᵢ` в будущем.

#
##
`𐒌ᨀ⠿⧙P₁, P₂, …, Pₙ⧘` ≔ ⠿ ⟨ `𐒌ᨀᵢ` из `𐒌ᨀ⠿` с номерами `P₁, P₂, …, Pₙ`  ⟩

##
`𐒌ᨀ⠿⧙P₁-Pₙ⧘` ≔ ⠿ ⟨ `𐒌ᨀᵢ` из `𐒌ᨀ⠿` с номерами от `P₁` до `Pₙ` включительно ⟩
